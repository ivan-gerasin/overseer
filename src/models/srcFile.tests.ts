describe('SrcFile', () => {

  const innerLevelFile = new File('core', )

  test('create instance', () => {

  })

  test('canImportFile return true for file with same level or lower')

  test('canBeExporterBy return true for file with same level or higher')
})